import 'package:flutter/material.dart';

class Ratingtabscreen  extends StatelessWidget {
  const Ratingtabscreen ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Ratings"),);
  }
}
